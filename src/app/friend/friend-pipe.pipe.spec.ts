import { FriendPipePipe } from './friend-pipe.pipe';

describe('FriendPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FriendPipePipe();
    expect(pipe).toBeTruthy();
  });
});
